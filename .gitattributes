# Normalize line endings across OSes (kills LF/CRLF warnings)
* text=auto

# Windows installers must keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must keep LF
*.sh text eol=lf

# Binaries
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.zip binary
*.exe binary
